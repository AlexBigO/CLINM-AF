command:
  print: True
  run: False

local:
  dir: # directory in which content will be copied
    name: /Users/abigot/CLINM/Calibration/CNAO_0425
    mkdir: False  # enforce (or not) creation of this dir
  content_renaming: null

remote:
  use_ssh_control_master: True  # if True, server needs to match the 'Host' (and not 'Hostname'!) in SSH Control Master
  username: &username abigot
  server: ui11
  type_of_content: directory  # possible values: directory, file
  content: [
    /work/desis/STIVI/dataWC/CNAO_0425/Run_03_Config1_Data_4_26_2025_Binary
  ]

# configuration to create a SSH Control Master for this server
ssh_control_master: 
  create: False
  ssh_config_file: /Users/abigot/.ssh/config
  host: ui11
  user: *username
  hostname: sbgui11
